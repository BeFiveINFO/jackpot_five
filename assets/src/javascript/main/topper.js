/*
	topper.js

	topper is the screen space right above the reels.
	In mobile layout, it appears when a designated button (Pay Table) is pressed.
*/
$(function() {
	"use strict";

	$('.flexslider').flexslider({
		animation: "slide",
		slideshowSpeed: 7000,
	});
});